# #did it myself to solve leetcode problem i.e. Given a string s consisting of words and spaces, return the length of the last word in the string.
# class Solution:
#     def lengthOfLastWord(self, s: str) -> int:
#         words = s.split()
#         len_s = len(words)
 
#         index_last = len_s - 1
#         last_word = words[index_last]
#         int_lastword = len(last_word)
#         return int_lastword

a = 123 

